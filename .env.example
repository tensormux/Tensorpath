# NeevPath environment variables.
# Copy to .env (gitignored) and fill in your values.

# Anthropic API key — used by the agentic Forge orchestrator.
# Get one at https://console.anthropic.com/ — set a spend limit before generating.
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
